<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <context:component-scan base-package="demo.controller"/>
    <mvc:annotation-driven/>
<!--    Spring MVC 中如果配置了 <mvc:annotation-driven>则所有的 Controller 就会被解析，-->
    <!--相应的 .do 请求就会被 Controller 处理 因此这个配置至关重要-->
    <!--当请求没有匹配到处理类（其中包括没有配置 <mvc:annotation-driven> 或者 访问的是静态资源文件）时-->
    <!--就会去找 <mvc:default-servlet-handler> （处理静态资源文件）配置的 DefaultServletHttpRequestHandler 默认处理器处理了-->
    <mvc:default-servlet-handler/>
    <!--    将静态资源(html jpg css js)还是由default-servlet处理(Tomcat config中的servlet)-->
    <!--    不然DispatcherServlet的路径也是/(全路径)而DispatcherServlet没有处理静态资源能力-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"></property>
        <property name="suffix" value=".html"/>
    </bean>
</beans>